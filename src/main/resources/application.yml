spring:
  datasource:
    url: jdbc:mysql://localhost:3306/culture?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: rkwhr123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
